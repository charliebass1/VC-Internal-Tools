Pull the latest changes from the remote branch onto the user's local machine. Run the following and output the commands clearly so the user can copy-paste them into their terminal:

1. Show the current branch name
2. Handle any untracked files that may conflict (suggest `git stash --include-untracked` or remove the specific file)
3. Pull with rebase: `git pull --rebase origin <current-branch>`

Output a single ready-to-run shell command the user can paste into their Mac terminal, tailored to the current branch name.
